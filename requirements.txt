# The chunker, metrics and figures need nothing beyond the standard library.
# These are for the full retrieval pipeline:  pip install -e ".[rag]"
chromadb>=0.5
sentence-transformers>=2.2
requests>=2.28
tqdm>=4.65
